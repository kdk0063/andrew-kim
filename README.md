# andrew-kim

Personal site. A single self-contained `index.html` — no build step, no
dependencies. Fonts come from Google Fonts; the two figures are embedded as
JPEG data URIs.

Deployed with GitHub Pages from `main` at the repository root.

The admin screenshot (FIG. 02) is rendered from fixtures, not live data.
